# jEnv
# status --is-interactive; and source (jenv init -|psub)
# set -x JAVA_HOME (jenv prefix)

# Google Cloud SDK.
# if [ -f '/Users/idol/Applications/google-cloud-sdk/path.fish.inc' ]; . '/Users/idol/Applications/google-cloud-sdk/path.fish.inc'; end

# Git Subrepo
# test -e {$HOME}/.gitsubrepo/git-subrepo/.fish.rc; and source {$HOME}/.gitsubrepo/git-subrepo/.fish.rc

# VS Code default editor
set -Ux EDITOR "code -w"
